/*
$('body').on('click', function(e) {
    var $this = $(this);
    e.preventDefault();

    if ( $('nav').hasClass('menu-open') ) {
        $('nav').removeClass('menu-open');
        $this.removeClass('active');
    } else {
        
    }
})
*/